<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:paddingEnd="4dp">

    <ImageView
        android:id="@+id/iv_episode_tv_show_poster"
        android:layout_width="0dp"
        android:layout_height="220dp"
        android:background="@drawable/bg_exp_poster_frame"
        android:scaleType="centerCrop"
        app:layout_constraintDimensionRatio="2:3"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <ProgressBar
        android:id="@+id/pb_episode_progress"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="0dp"
        android:layout_height="3dp"
        android:backgroundTint="#33FFFFFF"
        android:progressTint="@color/exp_accent"
        app:layout_constraintBottom_toBottomOf="@id/iv_episode_tv_show_poster"
        app:layout_constraintEnd_toEndOf="@id/iv_episode_tv_show_poster"
        app:layout_constraintStart_toStartOf="@id/iv_episode_tv_show_poster"
        tools:progress="80" />

    <TextView
        android:id="@+id/tv_episode_tv_show_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:fontFamily="@font/outfit"
        android:lines="1"
        android:textColor="@color/exp_mist"
        android:textSize="11sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/iv_episode_tv_show_poster"
        tools:text="TV show title" />

    <TextView
        android:id="@+id/tv_episode_info"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="4dp"
        android:ellipsize="end"
        android:fontFamily="@font/outfit"
        android:lines="2"
        android:textColor="@color/exp_ink"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/tv_episode_tv_show_title"
        tools:text="S# E# • Title" />

</androidx.constraintlayout.widget.ConstraintLayout>
