defaults.org=betterstreamflix
defaults.project=betterstreamflix
# Do NOT put the DSN public key here. For mapping/source uploads set either:
#   export SENTRY_AUTH_TOKEN=sntrys_…
# or auth.token=sntrys_… (org token with project:releases + org:read).
# CI uses secrets.SENTRY_AUTH_TOKEN.
